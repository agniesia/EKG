<Page
    x:Class="EKG.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EKG"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<Page.TopAppBar>
		<AppBar>
			<Grid x:Name="appgrig">
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>
				<Button x:Name="open" Content="OPEN" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="0" BorderBrush="#FFFF0101" Foreground="#FFFF0101" Height="200" FontSize="64" Background="Black" Click="open_Click"/>
			</Grid>
		</AppBar>
	</Page.TopAppBar>
	<Page.Foreground>
		<ImageBrush Stretch="Fill"/>
	</Page.Foreground>
	<Page.Background>
		<ImageBrush ImageSource="heart.jpg"/>
	</Page.Background>

    <Grid>
    	<Grid.Background>
    		<ImageBrush ImageSource="heart.jpg"/>
    	</Grid.Background>
    	<Grid.RowDefinitions>
    		<RowDefinition Height="0.2*"/>
    		<RowDefinition Height="0.6*"/>
    		<RowDefinition Height="0.2*"/>
    	</Grid.RowDefinitions>
    	<Viewbox x:Name="title">
    		<Button x:Name="ecgbutton" Content="ECG" Click="plot1_Click" BorderBrush="Black" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Stretch" FontSize="26.667"/>
    	</Viewbox>
    	<Canvas x:Name="plot" Background="Black" Margin="0,0,0,154" Grid.RowSpan="2" Grid.Row="1" />
    	<Slider x:Name="moove" Grid.Row="2" VerticalAlignment="Center" Background="Black" Foreground="#FFF30707" IsEnabledChanged="moove_IsEnabledChanged" BorderBrush="#FFF30707" ValueChanged="moove_ValueChanged"/>

    </Grid>
</Page>
